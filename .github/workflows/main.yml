name: keepalive
on:
  schedule:
    - cron: "*/10 13-23 * * 1-5"   # every 10 min, 9am-7pm ET, Mon-Fri
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -fsS https://mock-with-ai-backend.onrender.com/health
